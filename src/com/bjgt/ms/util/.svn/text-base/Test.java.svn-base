package com.bjgt.ms.util;

import java.awt.Image;
import java.awt.image.BufferedImage;
import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.File;
import java.io.FileInputStream;
import java.io.InputStream;

import javax.imageio.ImageIO;

import com.danga.MemCached.MemCachedClient;

public class Test {
	private static MemCachedClient cache = MemCacheService.getInstance();
	private static String gtkh = "gtkh";
	public static void main(String[] args) throws Exception {
		cache.delete("lskh");
//		File file = new File("C:\\Users\\Administrator\\Desktop\\img");
//		File[] listFiles = file.listFiles();
//		for (File f : listFiles) {
//			if (f.getName().endsWith("\\.svn")) {
//				continue;
//			}
//			InputStream is = new FileInputStream(f);
//			BufferedImage read = ImageIO.read(is);
//			System.out.println(read.getHeight() * 2);
//			System.out.println(read.getWidth() * 2);
//			read = ImageScaleUtil.getInstance().imageZoomOut(read,
//					read.getHeight() * 2, read.getWidth() * 2);
//			ImageIO.write(read, "gif", f);
//		}
	}

}
